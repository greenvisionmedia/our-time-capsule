name: Deploy
on:
  #push:
  workflow_dispatch:
jobs:
  deploy:
    uses: Green-Vision-Media/Actions/.github/workflows/gvm-deploy.yml@main
    with:
      staging: true
      domain: ourtimecapsule.org
      subdomain: otc
    secrets:
      test: ${{ secrets.TEST_PASSWORD }}
      ftp: ${{ secrets.FTP_PASSWORD }}
